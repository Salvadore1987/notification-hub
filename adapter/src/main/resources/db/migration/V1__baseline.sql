-- =====================================================================================
-- V1 — baseline схемы Notification Hub (DB-01: схема воспроизводима с нуля).
-- Схему comm_hub создаёт сам Flyway (spring.flyway.schemas), здесь — только
-- предпосылки, общие для последующих миграций.
-- Таблицы предметной области заводятся в Phase 4 (SRS §10.1).
-- =====================================================================================

-- pgcrypto: хеширование адресов в suppression_list и (по согласованию с ИБ) шифрование
-- контента сообщений (DB-04). Ставится в public, чтобы функции были видны всем схемам.
CREATE EXTENSION IF NOT EXISTS pgcrypto WITH SCHEMA public;

COMMENT ON SCHEMA comm_hub IS
    'Notification Hub: сообщения, конфигурация каналов/провайдеров/потоков, шаблоны, аудит.';
