/**
 * Collaborators shared by the use case services: the provider gateway that resolves an adapter from a
 * routed {@code ProviderRef} (MP-05, AR-04), the status notifier that records a transition and its
 * outbox event (ST-01, AD-03), and small stateless helpers.
 */
package uz.hamkorbank.commhub.application.service.support;
